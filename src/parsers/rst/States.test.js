import { RSTStateMachine } from './States';

test('construct', () => {
    let inputLines, document, inputOffset, matchTitles, inliner;
    const rstSm = new RSTStateMachine({ inputLines, document, inputOffset,
					matchTitles, inliner });
    

});
