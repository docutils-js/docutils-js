export class Source {
    constructor({ source, sourcePath, encoding }) {
        this.source = source;
        this.sourcePath = sourcePath;
        this.encoding = encoding;
    }
}

export default {
    Source,
};
